# torch and torchvision installed separately in Dockerfile with CPU-only version

# Core AI packages
transformers>=4.37.0
accelerate>=0.27.0
peft>=0.10.0
qwen-vl-utils

# Web framework
fastapi>=0.110.0
uvicorn[standard]>=0.28.0
python-multipart

# UI
gradio>=6.0.0

# PDF generation
reportlab>=4.0.0

# Image processing
pillow>=10.0.0
